# --- Roblox / Rojo build artifacts ---
# Plac NIE jest w repo (Studio-owned: mapa, RNG_MAP, assety, Units).
*.rbxl
*.rbxlx
*.rbxl.lock
*.rbxlx.lock
*.rbxm
*.rbxmx
# WYJATEK: backupy mapy/placu trzymamy w repo (place-owned, poza Rojo — jedyne wersjonowanie)
!place_backup/*.rbxm
build/
sourcemap.json

# --- Narzedzia / IDE ---
.rokit/
.vscode/
.DS_Store
Thumbs.db
*.log

# Paczki dostawcze / kopie zapasowe — lokalnie, poza repo
*.zip
_backup*/

# pliki referencyjne (lokalne, nie do repo)
og.png
oooo.mp4
*.mp4

# Python bytecode cache (tools/)
__pycache__/

# Artefakty reconcyliacji driftu (lokalny slad)
tools/_rescue/

# --- Mesh pipeline working files (lokalnie, poza repo — za ciezkie na git) ---
# GLB/FBX zrodlowe z Meshy, exporty, podglady; ~600MB, regenerowalne z Meshy/Blendera
mesh/
exports/
*.glb
